import {StyleSheet} from "react-native"
import {indexStyleVariables} from "../../../materials/styles";

export const homeStyles = StyleSheet.create({
    container: {
        backgroundColor: indexStyleVariables.defaultBg,
        flex: 1,
        display: "flex"

    }
})